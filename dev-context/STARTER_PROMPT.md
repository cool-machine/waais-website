# Starter Prompt — WAAIS Continuation

Copy the block below into a new LLM session when the current one ends. Update the "Likely next immediate step" line at the bottom whenever a slice ships so the next LLM picks up the right work.

```text
You are picking up an ongoing software project. Don't start coding until you have read the project context and verified the current state.

Project: WAAIS — Wharton Alumni AI Studio.
Project root: /Users/gg1900/coding/waais-website
Repository: https://github.com/cool-machine/waais-website
Owner: George (cool.lstm@gmail.com).

Read these files before doing anything else, in order:
1. /Users/gg1900/coding/waais-website/dev-context/PRODUCT.md          — what we're building (stable)
2. /Users/gg1900/coding/waais-website/dev-context/PLATFORM_MODEL.md   — data/access contract
3. /Users/gg1900/coding/waais-website/dev-context/DEV_CONTEXT.md      — past, present, future, working rules, session log
4. /Users/gg1900/coding/waais-website/dev-context/AZURE_PRODUCTION.md — Azure production deployment plan
5. /Users/gg1900/coding/waais-website/dev-context/PRIVACY_READINESS.md — privacy/legal launch checklist
6. /Users/gg1900/coding/waais-website/backend/README.md               — backend validation status & commands
7. /Users/gg1900/coding/waais-website/frontend/README.md              — frontend run/build/deploy commands

Then, before writing any code:
- Run git status; report current branch and whether the tree is clean.
- Verify /frontend, /backend, /mockups, /dev-context all exist.
- Summarize in 4–6 sentences what the project is, what's already shipped, and what the next slice is.
- Flag anything stale, contradictory, or risky in the docs.
- Wait for explicit instruction unless told to continue immediately.

Working rules (also documented in DEV_CONTEXT.md):
- Small slices, one concern per branch.
- After every code slice: composer validate --strict, php artisan test, php artisan migrate:fresh — all must pass before commit.
- Update DEV_CONTEXT.md (and STARTER_PROMPT.md whenever the next-slice direction shifts) in the same commit as the code.
- Commit → push branch → merge to main → push main at the end of every slice.
- Local dev/test stays on SQLite. Production target is Azure Database for PostgreSQL Flexible Server. Do not introduce Postgres-only SQL.
- If a slice would need human visual or manual testing to verify, stop and ask the user before continuing.

Likely next immediate step:
Continue Azure production setup from `AZURE_PRODUCTION.md`. Azure account context was verified on May 3, 2026 as `g1900@whartonaistudio.onmicrosoft.com` in subscription `Azure subscription 1` (`a66b1770-137e-49cc-a9c2-0ab3186e9752`) and tenant `9d7271ab-ab49-4b9b-a134-6905a15fdb38`; use only that organization account, not George's startup Azure account. Existing Azure resources in `rg-waais-prod-weu`: App Service plan `asp-waais-prod-weu-b1` (Linux Basic B1, West Europe), Web App `app-waais-api-prod-weu` (PHP 8.3, West Europe, default host `app-waais-api-prod-weu.azurewebsites.net`, HTTPS-only/Always On/HTTP2/FTPS-only enabled, basic auth disabled on both SCM and FTP, with `DB_*` settings already populated and pointing at the production database), Static Web App `swa-waais-prod-weu` (Free, West Europe, default host `proud-moss-0ec457703.7.azurestaticapps.net`, custom hostname `whartonai.studio` bound with managed cert), and PostgreSQL Flexible Server `psql-waais-prod-neu` (Burstable `Standard_B1ms`, PostgreSQL 16, 32 GiB auto-grow, 7-day backups, geo-redundant disabled, FQDN `psql-waais-prod-neu.postgres.database.azure.com`, admin user `waaisadmin`, public network access `Enabled` with the `AllowAllAzureServicesAndResourcesWithinAzureIps` firewall rule, application database `waais_production` created and migrated, state `Ready`). The database is in **North Europe** because the West Europe restriction is subscription-wide for Azure Database for PostgreSQL Flexible Server (verified via `az postgres flexible-server list-skus --location westeurope` returning empty `supportedServerEditions` with the explicit "Subscriptions are restricted from provisioning in this region" reason); both regions are inside the EU. The PostgreSQL admin password was rotated and written into App Service application settings — not stored elsewhere. Subscription-level monthly budget `waais-monthly-grant-pace` is live at amount `167` (Sponsorship subscription, USD) with notifications at 50/75/90/100% actual and 100% forecasted, sent to `cool.lstm@gmail.com` and `george@whartonai.studio`. Core Laravel App Service settings are populated: `APP_NAME=WAAIS`, `APP_ENV=production`, `APP_KEY` (base64 32-byte random, only in App Service), `APP_DEBUG=false`, `APP_URL=https://api.whartonai.studio`, `FRONTEND_URL=https://whartonai.studio`, `LOG_CHANNEL=stack`, `LOG_LEVEL=info`, `SESSION_DRIVER=database`, `SESSION_DOMAIN=.whartonai.studio`, `SESSION_SECURE_COOKIE=true`, `SANCTUM_STATEFUL_DOMAINS=whartonai.studio,api.whartonai.studio`, `FRONTEND_CORS_ORIGINS=https://whartonai.studio`. Backend is live in production: `https://api.whartonai.studio/up` returns HTTP 200, `/api/public/events` and `/api/public/startup-listings` return HTTP 200 with empty paginated envelopes (schema present, no published data yet). Custom domain `api.whartonai.studio` is bound to the App Service via App Service Managed Certificate (SNI, GeoTrust TLS RSA CA G1, valid through Nov 3, 2026, auto-renewing). DNS is on Cloudflare with CNAME `api → app-waais-api-prod-weu.azurewebsites.net` (proxy off / DNS only) and TXT `asuid.api`. Backend deploy pipeline is wired: Azure AD app `gh-waais-deploy` (appId `47ab24d1-5d10-477e-b493-c29728910f3d`) with federated OIDC trust on `cool-machine/waais-website`'s `main` branch, narrowly-scoped Contributor on the App Service, and `.github/workflows/deploy-backend.yml` that builds Composer (no dev), zips `backend/`, and zip-deploys via `azure/login@v2` + `azure/webapps-deploy@v3`. App Service startup command points to `/home/site/wwwroot/startup.sh`, which copies `backend/nginx-default.conf` over `/etc/nginx/sites-available/default` (so requests are routed through `public/`) and runs Laravel optimizations on container boot. The first production `php artisan migrate --force` ran on May 3, 2026 from inside the App Service container via `az webapp ssh` driven non-interactively by `expect` (Kudu's `/api/command` runs in the Kudu sidecar where PHP isn't installed, basic auth is off so publishing creds aren't an option, so `az webapp ssh` was the path); 16 migrations applied cleanly in batch 1, log saved to `/home/LogFiles/first-prod-migrate.log`. Laravel's TrustProxies middleware now honors the App Service load balancer's `X-Forwarded-Proto`/`X-Forwarded-Host` (added in `backend/bootstrap/app.php` with `at: '*'` since the container is only reachable through the platform LB), so pagination URLs and `Request::isSecure()` correctly use `https://api.whartonai.studio`. 187 backend tests / 822 assertions; deploy run `25279219627` shipped green.

**Frontend production deploy is shipped (May 3, 2026).** `https://whartonai.studio` serves the Vue SPA from SWA Free tier with managed TLS (CN=whartonai.studio, GeoTrust TLS RSA CA G1, valid through Nov 3, 2026); the SWA workflow `.github/workflows/deploy-frontend.yml` runs unit tests (142 passing), builds with `VITE_API_BASE_URL=https://api.whartonai.studio` and `VITE_BASE=/`, copies `frontend/staticwebapp.config.json` into `dist/`, and deploys via `Azure/static-web-apps-deploy@v1` using the GitHub secret `AZURE_STATIC_WEB_APPS_API_TOKEN`. Cloudflare apex DNS replaces the previous proxied A records with a single CNAME-flattened `@ → proud-moss-0ec457703.7.azurestaticapps.net` (DNS only / proxy off) plus the SWA validation TXT `_y48bhtx0kr31xyewwb1zu0vn17jetu2` at `@`. The hostname binding is `Ready` and the SWA-issued cert auto-renews. SPA navigation fallback (`/membership`, `/events`, etc.) returns 200 via `staticwebapp.config.json`, and CORS preflight from `https://whartonai.studio` returns 204 with `Access-Control-Allow-Origin: https://whartonai.studio`, so the production frontend can talk to `https://api.whartonai.studio` end-to-end. The GitHub Pages preview at `cool-machine.github.io/waais-website/` is preserved — `frontend/vite.config.js` reads `VITE_BASE` (defaults to `/waais-website/` for the GH Pages build, the SWA workflow overrides to `/`).

**Production Google OAuth + first super_admin shipped (May 3, 2026).** A Google Cloud project under George's organization Workspace account hosts the OAuth Client ID `350030962634-b4uu5cn12bjij2fb4tdk14s895rle8ti.apps.googleusercontent.com` (`WAAIS Production Web Client`, External, published with non-sensitive scopes only — `openid`, `userinfo.email`, `userinfo.profile` — so no Google verification queue applies). Authorized JavaScript origin: `https://whartonai.studio`. Authorized redirect URI: `https://api.whartonai.studio/auth/google/callback`. `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` are set as App Service application settings on `app-waais-api-prod-weu` and the secret lives only there. End-to-end sign-in works: `/app/sign-in` → Google → callback → user provisioned → `.whartonai.studio` session cookie → frontend hydrates with membership state. The first super_admin is user #1 `gvishiani@gmail.com` (`google_id=101174269836345463315`), promoted manually via an `expect`-driven `az webapp ssh` session running a base64-encoded PHP one-shot that bootstraps Laravel and saves `approval_status=Approved` / `permission_role=SuperAdmin` / `approved_at=now()`. The reusable PHP-in-container scaffold lives at `/tmp/waais-ops/run-php-in-app.expect` (ephemeral) — base64-encoding the script avoids the backslash-escaping collisions between Tcl, bash single-quoting, and PHP namespace separators that broke the first attempt with a `php -r` parse error. To add another super_admin, have them sign in via Google first to create the row, then run the same scaffold against their email; alternatively, once George is signed in as super_admin, `App\Http\Controllers\Api\Admin\AdminUserRoleController` provides in-app role management. The system user-context says `cool.lstm@gmail.com` but the actual signed-in Google account in production is `gvishiani@gmail.com` — admin notification emails will land there.

**Operational footgun (cache after appsettings set):** `az webapp config appsettings set` claims to restart the app, but in practice running PHP-FPM workers can keep serving the previous Laravel config cache for tens of seconds. After any `appsettings set`, always follow with an explicit `az webapp restart` and re-smoke before declaring the change live. The first OAuth smoke returned a 302 missing `client_id` and `redirect_uri` precisely because of this; an explicit restart fixed it.

**In-flight slice — ACS Email setup (started May 3, 2026, continuously updated below as steps land).** Goal: get production transactional email delivering from `whartonai.studio` so membership status, `events:send-reminders`, and `announcements:send-emails` actually reach inboxes. Plan: (1) register `Microsoft.Communication` resource provider; (2) create the Email Communication Services resource (`emailcomms-waais-prod`) and the Communication Services resource (`acs-waais-prod`) — ECS holds the domains, ACS holds the connection string for senders, both pinned to a Europe data location; (3) add an Azure-managed sender domain (instant, low-reputation `*.azurecomm.net`) to ECS for an immediate smoke path, and **in parallel** add custom domain `mail.whartonai.studio` and emit the verification + SPF + DKIM DNS records for George to add in Cloudflare; (4) connect the verified domain to ACS via `az communication email domain init-link`; (5) decide SMTP-relay vs ACS Email REST. SMTP relay requires an Entra app + XOAUTH2 (Symfony Mailer supports it but config is verbose). REST API path uses a tiny custom Laravel mailer — likely simpler given the existing TrustProxies-style minimal middleware; (6) write `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME='Wharton Alumni AI Studio'`, the chosen transport's settings (`MAIL_MAILER`, plus either `MAIL_HOST`/`MAIL_USERNAME`/`MAIL_PASSWORD` or `ACS_ENDPOINT`/`ACS_ACCESS_KEY`) into App Service settings; (7) `az webapp restart` (per the appsettings cache footgun); (8) smoke a real send to `gvishiani@gmail.com` via `php artisan tinker` over the same `expect`/SSH/base64-PHP scaffold used for the super_admin promotion. The slice after is the scheduler runner for `php artisan schedule:run` (likely an Azure Container Apps job or a small App Service WebJob), then final smoke checks in `AZURE_PRODUCTION.md`. Discourse remains deferred to the final stage.

**ACS Email progress log** (newest at top — overwrite when a step completes; this is the recovery breadcrumb):
- _Step 1 — provider registration:_ done. `Microsoft.Communication` is Registered on subscription `a66b1770-137e-49cc-a9c2-0ab3186e9752`.
- _Step 2 — ECS + ACS resources:_ done. `emailcomms-waais-prod` (Email Communication Service, dataLocation `Europe`, location `global`, state `Succeeded`) and `acs-waais-prod` (Communication Service, dataLocation `Europe`, hostName `acs-waais-prod.europe.communication.azure.com`, state `Succeeded`) live in `rg-waais-prod-weu`.
- _Step 3 — Azure-managed sender domain + linkage:_ done. Domain `AzureManagedDomain` under `emailcomms-waais-prod` (state `Succeeded`); `fromSenderDomain = mailFromSenderDomain = b513a906-9280-42e3-9601-21e033722c36.azurecomm.net`. The default sender username is `DoNotReply`. Domain ARM ID is linked into `acs-waais-prod.linkedDomains` so ACS can authorize sends through it.
- _Step 4 — Entra app for SMTP auth:_ done. `acs-smtp-waais-prod` (appId `a8e79a7d-cf52-454c-9a47-68bc2994a35b`, SP objectId `c68351c9-325b-475d-828a-91d5b2e11fd5`, tenant `9d7271ab-ab49-4b9b-a134-6905a15fdb38`, sign-in audience `AzureADMyOrg`). Client secret was minted with `az ad app credential reset --years 2`; the secret value lives only in App Service settings as `ACS_MAIL_PASSWORD` (not in the repo). The SP holds `Contributor` scoped narrowly to the ACS resource (`/subscriptions/a66b1770-137e-49cc-a9c2-0ab3186e9752/resourceGroups/rg-waais-prod-weu/providers/Microsoft.Communication/CommunicationServices/acs-waais-prod`), not the whole RG.
- _Step 5 — App Service settings:_ done. Wrote `MAIL_MAILER=azure_communication_services`, `ACS_MAIL_HOST=smtp.azurecomm.net`, `ACS_MAIL_PORT=587`, `ACS_MAIL_USERNAME=acs-waais-prod.a8e79a7d-cf52-454c-9a47-68bc2994a35b.9d7271ab-ab49-4b9b-a134-6905a15fdb38` (the canonical ACS SMTP username pattern: `<acs-resource>.<entra-app-id>.<entra-tenant-id>`), `ACS_MAIL_PASSWORD=<entra-secret>`, `MAIL_FROM_ADDRESS=DoNotReply@b513a906-9280-42e3-9601-21e033722c36.azurecomm.net`, `MAIL_FROM_NAME='Wharton Alumni AI Studio'`. The `azure_communication_services` mailer is already wired in `backend/config/mail.php` (Symfony Mailer SMTP transport with the `ACS_MAIL_*` env names). Followed `appsettings set` with an explicit `az webapp restart` per the documented cache footgun.
- _Step 6 — smoke send:_ Laravel side green. `php /tmp/run.php` echoed `MAILER=azure_communication_services / FROM=DoNotReply@b513a906-9280-42e3-9601-21e033722c36.azurecomm.net / HOST=smtp.azurecomm.net / SMOKE_SEND_OK`. The Symfony Mailer SMTP transport authenticated against `smtp.azurecomm.net:587` with the `<acs-resource>.<entra-app-id>.<entra-tenant-id>` username pattern and the Entra app's client secret as password, and ACS accepted the message. Inbox-level confirmation pending — George needs to check `gvishiani@gmail.com` (likely in Promotions/Spam given the Azure-managed domain's neutral reputation).
- _Step 7 — custom domain `mail.whartonai.studio`:_ deferred to next slice. The Azure-managed `*.azurecomm.net` works for v1 but has poor sender reputation (likely Spam/Promotions in Gmail). Custom domain requires SPF/DKIM/MX TXT records in Cloudflare; planned, not blocking.
```

## Maintenance

Keep this file in sync with `DEV_CONTEXT.md` whenever:

- A slice ships and the next-slice direction changes — update the "Likely next immediate step" paragraph.
- The list of context files changes — update the read order.
- Working rules change — update the rules section.
